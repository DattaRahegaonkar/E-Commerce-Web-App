
kind: Service
apiVersion: v1
metadata:
  name: frontend-service
  namespace: ecommerce
  annotations:
    argocd.argoproj.io/sync-wave: "9"
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
