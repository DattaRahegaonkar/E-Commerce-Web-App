# MongoDB Connection
# ------------------
# Local MongoDB (for development)
MONGO_URI=mongodb://localhost:27017/userdb

# Cloud MongoDB (uncomment and update credentials for production)
MONGO_URI=mongodb://ecommerceuser:ecommerce123@mongodb:27017/ecommerceDB
# JWT Configuration
# ----------------
# Secret key for signing JWTs (generate a strong random string for production!)
JWT_SECRET=use-a-strong-random-string-in-production

# Token expiration time
JWT_EXPIRES_IN=7d

# Server Configuration
# -------------------
# Port for the server to listen on
PORT=8081

# Environment (development, production, test)
# Set to 'production' for production deployment
NODE_ENV=development

# CORS Configuration
# -----------------
# Frontend URL for CORS (add multiple comma-separated values if needed)
ALLOWED_ORIGINS=http://localhost:5173,https://your-production-frontend-url.com,http://localhost:5173,http://localhost:80,http://localhost:8081,http://localhost,http://72.60.111.1:80,http://72.60.111.1/,http://ecommerce.rahegaonkar.site
